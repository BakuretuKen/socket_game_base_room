<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>simple code share</title>
    <link rel="shortcut icon" href="/favicon.ico">
    <meta name="robots" content="noindex,nofollow">
    <link rel="stylesheet" href="/dist/simple.min.css">
    <style>
        body>header h1 {
            margin-top: 0px;
            margin-bottom: 0px;
        }
        button.debug {
            padding: 0.3rem 0.6rem;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <header>
        <h1>GAME PAGE</h1>
    </header>
    <main>
        <div id="message" style="padding: 10px; margin-bottom: 20px; background-color: #e8f5e8; border: 1px solid #4caf50; border-radius: 4px; color: #2e7d32; display: none;"></div>
        <div id="errorMessage" class="error" style="display: none; padding: 10px; margin-bottom: 20px; background-color: #ffebee; border: 1px solid #f44336; border-radius: 4px; color: #c62828;"></div>
        <input type="hidden" id="gameCode" value="<%= gameCode %>">
        <input type="hidden" id="userName" value="<%= userName %>">
        <input type="hidden" id="master" value="<%= master %>">

        <!-- 待機ルーム -->
        <div id="waitingRoom">
            <h3>待機ルーム</h3>
            <ol id="waitingRoomList"></ol>
        </div>

        <hr/>
        <!-- デバッグ用 -->
        <div id="debugButtons">
            DEBUG：
            <button class="debug" onclick="console.log(players);">players</button>
            <button class="debug" onclick="console.log(playerName);">playerName</button>
            <button class="debug" onclick="console.log(isMaster);">isMaster</button>
            | <input type="text" id="debugMessage" value="debug" style="height: 30px;"> <button class="debug" onclick="sendMessage({action: 'SEND_DEBUG', message: document.getElementById('debugMessage').value});">send</button>
        </div>
    </main>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/dist/views/game_base.js"></script>
    <!-- マスターJS、プレイヤーJSを切り替える場合 -->
    <% if (master == 1) { %>
        <script src="/dist/views/game_master.js"></script>
    <% } else { %>
        <script src="/dist/views/game_player.js"></script>
    <% } %>

    <!-- マスターJS、プレイヤーJSを切り替えない場合 -->
    <!-- <script src="/dist/views/game.js"></script> -->
</body>
</html>
